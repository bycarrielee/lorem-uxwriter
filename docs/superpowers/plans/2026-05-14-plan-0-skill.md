# Phase 0: Lorem as a Claude Skill

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Package Lorem as a Claude Code skill so practitioners can use the UX writing agent immediately — before the web app is deployed. Practitioners install the skill into `~/.claude/skills/`, then invoke `/lorem-uxwriter` from any project.

**Relationship to the web app:** The content in `lorem-uxwriter/` (foundations, patterns, copy entries) seeds the web app database when it launches. Phase 0 uses it as a skill; the web app imports it. No content is thrown away.

**How it works:** The skill instructs Claude to read local library files in a deterministic order before responding. Library lookup happens via file reads, not database queries. Everything else — review vs generate detection, source labels, rationale, confidence — works the same way as the web app agent.

---

## Repository structure

```
lorem-uxw/
└── lorem-uxwriter/                 ← complete installable package
    ├── SKILL.md                    ← agent instructions
    ├── SETUP.md                    ← install guide
    ├── foundations/
    │   ├── global/
    │   │   ├── voice.md
    │   │   ├── style.md
    │   │   ├── accessibility.md
    │   │   ├── localisation.md
    │   │   ├── singapore-government-agencies.md
    │   │   └── singapore-government-schemes-services.md
    │   └── products/
    ├── patterns/
    │   ├── global/
    │   │   ├── buttons.md
    │   │   ├── errors.md
    │   │   ├── forms.md
    │   │   ├── alerts.md
    │   │   ├── modals.md
    │   │   ├── links.md
    │   │   ├── long-form.md
    │   │   ├── states.md
    │   │   ├── push-notifications.md
    │   │   └── release-notes.md
    │   └── products/
    └── library/
        ├── global/
        │   └── form-labels.json
        └── products/
```

## Installed file structure

```
~/.claude/skills/lorem-uxwriter/
├── SKILL.md
├── SETUP.md
├── foundations/
│   ├── global/
│   │   ├── voice.md
│   │   ├── style.md
│   │   ├── accessibility.md
│   │   ├── localisation.md
│   │   ├── singapore-government-agencies.md
│   │   └── singapore-government-schemes-services.md
│   └── products/
├── patterns/
│   ├── global/
│   │   ├── buttons.md
│   │   ├── errors.md
│   │   ├── forms.md
│   │   ├── alerts.md
│   │   ├── modals.md
│   │   ├── links.md
│   │   ├── long-form.md
│   │   ├── states.md
│   │   ├── push-notifications.md
│   │   └── release-notes.md
│   └── products/
└── library/
    ├── global/
    │   └── form-labels.json
    └── products/
```

---

## Status

| Area | Status |
|------|--------|
| `lorem-uxwriter/foundations/global/` — 6 files | ✅ Done |
| `lorem-uxwriter/patterns/global/` — 10 files | ✅ Done |
| `lorem-uxwriter/library/global/form-labels.json` | ✅ Done |
| `lorem-uxwriter/SKILL.md` | ✅ Done |
| `lorem-uxwriter/SETUP.md` | ⬜ To do (Task 1) |

---

## Task 1: Write SETUP guide

Create `lorem-uxwriter/SETUP.md`:

```markdown
# Lorem skill — setup guide

This guide installs Lorem as a Claude Code skill on your machine. Once installed, invoke it with `/lorem-uxwriter` from any project in Claude Code.

## Requirements

- Claude Code installed and configured
- Access to your `~/.claude/skills/` directory

## Install

1. From the repository root, run:

   ```bash
   cp -r lorem-uxwriter ~/.claude/skills/lorem-uxwriter
   ```

   The installed structure should look like:

   ```
   ~/.claude/skills/lorem-uxwriter/
   ├── SKILL.md
   ├── SETUP.md
   ├── foundations/
   │   ├── global/
   │   └── products/
   ├── patterns/
   │   ├── global/
   │   └── products/
   └── library/
       ├── global/
       └── products/
   ```

2. Verify Claude Code recognises the skill. Open any project in Claude Code and run:

   ```
   /lorem-uxwriter Hello
   ```

   You should see a response from the Lorem agent.

## Update after content changes

When files change in the repository, re-run the install command to sync:

```bash
cp -r lorem-uxwriter ~/.claude/skills/lorem-uxwriter
```

## Add product overrides

To override global foundations or patterns for a specific product, create override files in `skill/`:

```
skill/foundations/products/my-product/voice.md
skill/patterns/products/my-product/buttons.md
```

The file only needs to contain the overrides — Lorem reads the product file first and falls back to global for anything not overridden.

Re-run the install command to pick up the changes.

## Add product library entries

To add a product copy library, create a folder under `skill/library/products/`:

```
skill/library/products/my-product/copy-entries.json
```

The file must be a JSON array. Minimum required fields per entry:

```json
[
  {
    "id": "my-product-001",
    "element_type": "errors",
    "copy": "Enter a valid email address.",
    "context": "Inline error on the email field.",
    "status": "active"
  }
]
```

Re-run the install command, then invoke Lorem with the product name:

```
/lorem-uxwriter [your copy]
Product: My Product
```

## Feedback

Report issues or suggest improvements by raising them with the Lorem project team.
```

---

## Self-review checklist

| Check | Task |
|---|---|
| `lorem-uxwriter/library/global/form-labels.json` exists | Task 1 |
| SKILL.md workflow references correct paths (`foundations/global/`, `patterns/global/`, `library/global/`, `library/products/`) | ✅ Done |
| SKILL.md workflow steps numbered correctly (Steps 1–8, no missing or duplicate steps) | ✅ Done |
| SKILL.md lists all 10 pattern types in element type options | ✅ Done |
| SETUP guide install command is `cp -r lorem-uxwriter ~/.claude/skills/lorem-uxwriter` (from repo root) | Task 1 |
| SETUP guide includes update, product override, and product library instructions | Task 1 |
| Invoking `/lorem-uxwriter` with a copy string produces a response with source label, suggestion, rationale, confidence | Manual test after install |

---

*Plan written: 2026-05-14 · Updated: 2026-05-21*
